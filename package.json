{
  "name": "studyenglish",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm-run-all --parallel start:**",
    "start:middleware": "cd middleware && npm run watch",
    "start:client": "cd client && npm start",
    "server:core": "f: && cd F:\\server_backup\\cms && npm run develop"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
