{
  "name": "studyenglish",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm-run-all --parallel start:**",
    "start:middleware": "cd middleware && npm run watch",
    "start:client": "cd client && npm start",
    "core": "i: && cd i:\\server_backup\\cms && npm run develop"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "natural": "^6.6.0",
    "npm-run-all": "^4.1.5"
  }
}
